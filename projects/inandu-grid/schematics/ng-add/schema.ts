export interface NgAddSchematicOptions {
  /** Skip generating the example component; just print next steps. */
  skipExample: boolean;
  /** Name for the generated example component. */
  name: string;
}
